machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t caerusassociates/mordecai .
test:
  override:
    - docker run -w /src/resources caerusassociates/mordecai py.test
